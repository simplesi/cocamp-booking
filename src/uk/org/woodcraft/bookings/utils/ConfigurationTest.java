package uk.org.woodcraft.bookings.utils;

import org.junit.Test;


public class ConfigurationTest {
	@Test
	public void testConfig()
	{
//		Configuration config = Configuration.get();
		
	// TODO: Make this actually test something. The below is stupid because we don't determine prod vs dev anywhere in the actual code.	
//		assertEquals("cocamp-local", config.getProperty(Configuration.ENVIRONMENT));
		
//		assertEquals("http://localhost:8888/", config.getProperty("baseurl"));
		
	}
}
